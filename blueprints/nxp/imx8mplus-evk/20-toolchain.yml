#
# 20-toolchain.yml
# Install aarch64 cross-compiler for application builds (from catalog).
#
- name: "Create toolchain destination"
  action: "run_command"
  reason: "mkdir has no dedicated primitive; pre-creating the destination directory ensures subsequent archive extractions land in the correct location."
  command: "mkdir -p /opt/imx8mplus/toolchain"
  creates: "/opt/imx8mplus/toolchain"

- name: "Install aarch64 Linux toolchain"
  action: "unarchive_from_ref"
  ref: "toolchain.arm-gnu.aarch64-none-linux-gnu@stable"
  dest: "/opt/imx8mplus/toolchain"
  strip_components: 1
  creates: "/opt/imx8mplus/toolchain/bin/aarch64-none-linux-gnu-gcc"
  mode: "USER_RWX GROUP_RX OTHER_RX"
  owner: "root:root"

- name: "Add toolchain to PATH"
  action: "write_env_file"
  file: "/etc/profile.d/20-imx8-toolchain.sh"
  content: |
    #!/bin/sh
    export PATH=$PATH:/opt/imx8mplus/toolchain/bin
    export CROSS_COMPILE=aarch64-none-linux-gnu-
  mode: "USER_RWX GROUP_RX OTHER_RX"
